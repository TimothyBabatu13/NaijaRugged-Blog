import Login from "./Login";

const Admin = (props)=>{
    //props.category
    
    return(
        <>
            <Login className={props.className}/>
        </>
    )
}

export default Admin;